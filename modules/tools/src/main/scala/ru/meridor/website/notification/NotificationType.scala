package ru.meridor.website.notification

/**
 * A set of enumeration constants corresponding to different notification types
 */
object NotificationType extends Enumeration{
  type NotificationType = Value
  val SMS/*, Email, Message*/ = Value //Various notification types
}
